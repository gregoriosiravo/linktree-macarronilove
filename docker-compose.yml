services:
  macarroni-love:
    build:
      context: .
      target: runtime
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NUXT_PUBLIC_SITE_URL: http://localhost:3000
    restart: unless-stopped
